################################################################################
# /packages/telegram_notifications.yaml
################################################################################
#
# Этот пакет Home Assistant реализует интеграцию с Telegram для отправки
# различных уведомлений, обеспечивая мгновенное оповещение пользователей
# о событиях в «умном доме». Он включает:
#
# Скрипты:
#    • Формирование и отправка ежедневного прогноза погоды
#
# Автоматизации, отправляющие сообщения при:
#    • Запуске/выключении Home Assistant.
#    • Открытии входной двери в отсутствие владельца.
#    • Прогнозе погоды каждый день.
#    • Звонке в дверь.
#    • Включении камеры (в определённое время/дни).
#    • Завершении стирки.
#    • Необходимости проветрить помещение при высоком CO2.
#    • Завершении проветривания и нормализации уровня CO2.
#    • Понижении температуры в помещениях при открытой двери/окне.
#    • Низком уровне заряда батарей в устройствах (с помощью blueprint).
#    • Доступных обновлениях компонентов/плагинов.
#    • Необходимости замены фильтра в кондиционере.
#
# ВАЖНО:
# Везде ниже используется notify-сущность notify.telegram_user_id.
# ЗАМЕНИТЕ её на фактическую сущность, созданную интеграцией Telegram Bot
# (Инструменты разработчика → Состояния → фильтр notify.*).
################################################################################

##############################################################################
#                                  СКРИПТЫ
##############################################################################

# ----------------------------------------------------------------------------
# Формирование и отправка прогноза погоды
# ----------------------------------------------------------------------------

script:
  telegram_send_weather_forecast:
    description: "Формирует и отправляет прогноз погоды указанному получателю"
    fields:
      recipient:
        description: "Сущность notify.* (например, notify.telegram_bot_5861567452_382517)"
        example: "notify.telegram_bot_5861567452_382517"
        required: true
    sequence:
      # Сначала получаем прогноз через сервис
      - service: weather.get_forecasts
        data:
          type: hourly # или daily, в зависимости от того, что вам нужно
        target:
          entity_id: weather.kvartira_133
        response_variable: weather_forecast

      - variables:
          today_date: "{{ now().strftime('%d.%m.%Y') }}"
          title: "<b>Прогноз погоды на <u>{{ today_date }}</u></b>"

          # Текущие погодные условия
          current_condition: "{{ states('sensor.open_meteo_opisanie_na_russkom') | default(states('weather.kvartira_133')) }}"
          current_temperature: "{{ state_attr('weather.kvartira_133', 'temperature') | default('N/A') }}"
          current_wind_bearing: "{{ state_attr('weather.kvartira_133', 'wind_bearing') | default(0) }}"
          current_wind_speed: "{{ state_attr('weather.kvartira_133', 'wind_speed') | default('N/A') }}"
          current_pressure_mmhg: "{{ state_attr('weather.kvartira_133', 'pressure') | default(0) }}"

          # Преобразуем направление ветра в текст
          wind_direction: >-
            {% set bearing = current_wind_bearing | int %}
            {% if bearing >= 337.5 or bearing < 22.5 %}северный
            {% elif bearing >= 22.5 and bearing < 67.5 %}северо-восточный
            {% elif bearing >= 67.5 and bearing < 112.5 %}восточный
            {% elif bearing >= 112.5 and bearing < 157.5 %}юго-восточный
            {% elif bearing >= 157.5 and bearing < 202.5 %}южный
            {% elif bearing >= 202.5 and bearing < 247.5 %}юго-западный
            {% elif bearing >= 247.5 and bearing < 292.5 %}западный
            {% elif bearing >= 292.5 and bearing < 337.5 %}северо-западный
            {% else %}неизвестно{% endif %}

          # Берём скорректированное давление из сенсора или из weather entity
          current_pressure: >-
            {% set sensor_pressure = states('sensor.livingroom_corrected_pressure') | float(0) %}
            {% if sensor_pressure > 0 %}
              {{ sensor_pressure | round(1) }}
            {% elif current_pressure_mmhg > 0 %}
              {{ current_pressure_mmhg | round(1) }}
            {% else %}
              735
            {% endif %}

          # Выбор иконки для текущего состояния
          weather_icon: >-
            {% set cond = states('weather.kvartira_133') %}
            {% if cond == 'sunny' or current_condition == 'ясно' %}☀️
            {% elif cond == 'partlycloudy' or current_condition == 'малооблачно' %}⛅
            {% elif cond == 'cloudy' or current_condition in ['облачно с прояснениями', 'облачно'] %}☁️
            {% elif cond == 'overcast' or current_condition == 'пасмурно' %}🌥
            {% elif cond == 'fog' or current_condition == 'туман' %}🌫
            {% elif cond in ['drizzle','light-rain'] or current_condition in ['морось', 'небольшой дождь'] %}🌦
            {% elif cond in ['rainy','rain'] or current_condition == 'дождливо' %}🌧
            {% elif cond == 'pouring' or current_condition == 'ливень' %}⛈
            {% elif cond == 'snowy' or current_condition == 'снег' %}❄️
            {% elif cond == 'snowy-heavy' or current_condition == 'сильный снег' %}🌨
            {% elif cond == 'sleet' or current_condition == 'дождь со снегом' %}🌧❄️
            {% elif cond == 'lightning' or current_condition == 'гроза' %}⚡
            {% else %}🌈{% endif %}

      # Отправляем сообщение через новый универсальный сервис notify.send_message
      - action: notify.send_message
        target:
          entity_id: "{{ recipient }}"
        data:
          title: "{{ title }}"
          message: >-
            {{ weather_icon }} <b>Сейчас:</b> {{ current_condition }}

            🌡 <b>Температура:</b> {{ current_temperature }}°C
            🌬 <b>Ветер:</b> {{ wind_direction }}, {{ current_wind_speed }} м/с

            <b>📅 Прогноз на сегодня:</b>
            {%- if weather_forecast is defined and weather_forecast['weather.kvartira_133'] is defined and weather_forecast['weather.kvartira_133']['forecast'] is defined %}
            {%- set forecasts = weather_forecast['weather.kvartira_133']['forecast'] %}
            {%- set today_items = namespace(found=false) %}
            {%- for forecast in forecasts[:8] %}
            {%- if forecast.datetime is defined %}
            {%- set forecast_dt = forecast.datetime | as_datetime | as_local %}
            {%- if forecast_dt is not none and forecast_dt.date() == now().date() and forecast_dt > now() %}
            {%- set today_items.found = true %}
            {%- set cond = forecast.condition | default('unknown') %}
            {%- set icon = '☀️' if cond == 'sunny' else '⛅' if cond == 'partlycloudy' else '☁️' if cond == 'cloudy' else '🌥' if cond == 'overcast' else '🌫' if cond == 'fog' else '🌦' if cond in ['drizzle','light-rain'] else '🌧' if cond in ['rainy','rain'] else '⛈' if cond == 'pouring' else '❄️' if cond == 'snowy' else '🌨' if cond == 'snowy-heavy' else '🌧❄️' if cond == 'sleet' else '⚡' if cond == 'lightning' else '🌈' %}
            • <b>{{ forecast_dt.strftime('%H:%M') }}</b> {{ icon }} {{ forecast.temperature | round(1) }}°C
            {%- set precip = forecast.precipitation | float(0) if forecast.precipitation is defined else 0 -%}
            {%- if precip > 0 %}, осадки: {{ precip }} мм{% endif -%}
            {%- endif -%}
            {%- endif -%}
            {%- endfor %}
            {%- if not today_items.found %}
            ℹ️ Прогноз на оставшуюся часть дня недоступен
            {%- endif -%}
            {%- else %}
            ⚠️ Прогноз недоступен (проверьте интеграцию погоды)
            {%- endif %}

            <b>💨 Атмосферное давление:</b> {{ current_pressure }} мм рт. ст.
            (<i>{% if current_pressure > 740 %}⬆️ выше нормы{% elif current_pressure < 738 %}⬇️ ниже нормы{% else %}🟢 нормальное{% endif %}</i>)

##############################################################################
#                             АВТОМАТИЗАЦИИ
##############################################################################
automation:
  # ----------------------------------------------------------------------------
  # Уведомление: Прогноз погоды
  # ----------------------------------------------------------------------------
  - id: "ac0837cf-9ecb-413b-96fb-89552934212d"
    alias: "Уведомление. Телеграм: Прогноз погоды"
    description: "Отправляет прогноз погоды на сегодня в Телеграм каждое утро."
    mode: single
    triggers:
      - trigger: time
        at: "07:45:00"
    actions:
      - action: script.telegram_send_weather_forecast
        data:
          recipient: notify.telegram_bot_5861567452_382517

  # ----------------------------------------------------------------------------
  # Уведомление: Открытие входной двери
  # ----------------------------------------------------------------------------
  - id: "1f7e3ae4-268e-4afa-a7e6-d395821f244b"
    alias: "Уведомление. Телеграм: Открытие входной двери"
    description: "Отправляет уведомление об открытии двери, если master не дома и режим уборки выключен."
    mode: single
    triggers:
      - trigger: device
        device_id: abd3e40b49cfbe61411bac5d87bf000f
        entity_id: binary_sensor.hallway_door_contact
        domain: binary_sensor
        type: opened
    conditions:
      - condition: state
        entity_id: person.master
        state: "not_home"
      - condition: state
        entity_id: input_boolean.bathroom_cleaning_mode
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: "⚠️ <b>Открытие входной двери!</b> 🔓"

  # ----------------------------------------------------------------------------
  # Уведомление: Звонок в дверь
  # ----------------------------------------------------------------------------
  - id: "eaddf6dd-762d-487e-9bf2-ba018f10b742"
    alias: "Уведомление. Телеграм: Уведомление о звонке в дверь"
    mode: single
    triggers:
      - trigger: device
        domain: mqtt
        device_id: d2c3001cdc59660c456de45aec09da91
        type: action
        subtype: single
      - trigger: state
        entity_id: event.madv_cn_329651133_miowlv2l_doorbell_ring_e_2_1
    conditions:
      - condition: state
        entity_id: person.master
        state: "not_home"
      - condition: state
        entity_id: input_boolean.bathroom_cleaning_mode
        state: "off"
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
      - condition: state
        entity_id: input_boolean.doorbell_spam_guard
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: "⚠️ <b>Кто-то звонит в дверь</b>"
      - action: input_boolean.turn_on
        target:
          entity_id: input_boolean.doorbell_spam_guard
      - delay: "00:00:30"
      - action: input_boolean.turn_off
        target:
          entity_id: input_boolean.doorbell_spam_guard

  # ----------------------------------------------------------------------------
  # Уведомление: Сервер запущен
  # ----------------------------------------------------------------------------
  - id: "686a3440-7629-4b55-b508-593caf3679af"
    alias: "Уведомление. Телеграм: Сервер запущен"
    description: "Уведомление о запуске сервера"
    mode: single
    triggers:
      - trigger: homeassistant
        event: start
    actions:
      - variables:
          ha_start_text: >
            {{ ["Home assistant запущена!", "Home assistant теперь работает!", "Home assistant has begun!", "HA запущен!"] | random }}
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: "⚠️ {{ ha_start_text }}"
      # - action: telegram_bot.send_photo
      #   data:
      #     file: "/config/www/images/notify_alert/ha_wakeup.png"
      #     caption: "{{ ha_start_text }}"
      # при необходимости можно добавить parse_mode: html

  # ----------------------------------------------------------------------------
  # Уведомление: Сервер отключён
  # ----------------------------------------------------------------------------
  - id: "c8dc47aa-90f0-4522-b71e-4e99bbfe6c76"
    alias: "Уведомление. Телеграм: Сервер отключён"
    description: "Уведомление о выключении сервера"
    mode: single
    triggers:
      - trigger: homeassistant
        event: shutdown
    actions:
      - variables:
          ha_shutdown_text: >
            {{ ['Home Assistant выключен', 'Выключение Home Assistant', 'HA будет отключён'] | random }}
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: "⚠️ {{ ha_shutdown_text }}"
      # - action: telegram_bot.send_photo
      #   data:
      #     file: "/config/www/images/notify_alert/ha_shutdown.png"
      #     caption: "{{ ha_shutdown_text }}"

  # ----------------------------------------------------------------------------
  # Уведомление: Возможны обновления компонентов
  # ----------------------------------------------------------------------------
  - id: "4c5c8e57-be5b-4380-88a1-4ecc730c19da"
    alias: "Уведомление. Телеграм: Возможно обновления компонента системы"
    description: "Оповещение о новых обновлениях компонентов Home Assistant"
    mode: single
    triggers:
      - trigger: state
        entity_id:
          - update.file_editor_update
          - update.home_assistant_core_update
          - update.home_assistant_google_drive_backup_update
          - update.home_assistant_operating_system_update
          - update.home_assistant_supervisor_update
          - update.log_viewer_update
          - update.mosquitto_broker_update
          - update.studio_code_server_update
          - update.advanced_ssh_web_terminal_update
          - update.zigbee2mqtt_update
          - update.ps5_mqtt_update
        to: "on"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: >
            {% set entity = trigger.to_state %}
            {% if entity %}
              🆕 Обновление доступно: {{ entity.attributes.friendly_name | default('Неизвестный компонент') }}
              Текущая версия: {{ entity.attributes.installed_version | default('недоступно') }}
              Новая версия: {{ entity.attributes.latest_version | default('недоступно') }}
              📝 Сводка: {{ entity.attributes.release_summary | default('Нет подробностей') }}
            {% else %}
              Ошибка: Данные обновления недоступны.
            {% endif %}

  # ----------------------------------------------------------------------------
  # Уведомление: Низкий заряд батареи массажёра
  # ----------------------------------------------------------------------------
  - id: "ab45665b-4995-448d-b180-c0e1d77184e9"
    alias: "Уведомление. Телеграм: Низкий заряд батареи массажёра"
    mode: single
    triggers:
      - trigger: numeric_state
        entity_id: sensor.soocare_cn_1083009681_m14_battery_level_p_3_1
        below: 30
    conditions:
      - condition: state
        entity_id: person.master
        state: "home"
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: "⚠️ <b>Массажёр нуждается в зарядке!</b>"

  # ----------------------------------------------------------------------------
  # Уведомление: Проблемы с жесткими дисками Proxmox
  # ----------------------------------------------------------------------------
  - id: "daa17acf-7c8a-4a8e-9fd0-4f2c5348c814"
    alias: "Уведомление. Телеграм: Проблемы с жесткими дисками Proxmox"
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.disk_pve_h_w_raid1_dev_sdb_health
        to: "on"
      - trigger: state
        entity_id: binary_sensor.disk_pve_128gb_ssd_dev_sda_health
        to: "on"
    conditions:
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: >-
            ⚠️ <b>Обнаружена проблема с жестким диском Proxmox!</b>
            {% if trigger.entity_id == 'binary_sensor.disk_pve_h_w_raid1_dev_sdb_health' %}
            🔴 Внешний RAID массив (sdb) требует внимания!
            {% elif trigger.entity_id == 'binary_sensor.disk_pve_128gb_ssd_dev_sda_health' %}
            🔴 Основной диск системы (sda) требует внимания!
            {% endif %}

  # ----------------------------------------------------------------------------
  # Уведомление: Высокая загрузка диска Proxmox
  # ----------------------------------------------------------------------------
  - id: "4751a90b-0134-4906-84f0-e5b1a7cb02c2"
    alias: "Уведомление. Телеграм: Высокая загрузка диска Proxmox"
    mode: single
    triggers:
      - trigger: numeric_state
        entity_id: sensor.node_pve_disk_used_percentage
        above: 90
    conditions:
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: >-
            ⚠️ <b>Предупреждение: Высокая загрузка диска на Proxmox!</b>
            Использование диска: {{ states('sensor.node_pve_disk_used_percentage') }}%.

  # ----------------------------------------------------------------------------
  # Уведомление: Доступны обновления для Proxmox
  # ----------------------------------------------------------------------------
  - id: "ab5c1bd2-8756-4736-9909-52766bf989bd"
    alias: "Уведомление. Телеграм: Доступны обновления для Proxmox"
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.node_pve_updates_packages
        to: "on"
    conditions:
      - condition: state
        entity_id: person.master
        state: "home"
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: >-
            🔄 <b>Доступны обновления для Proxmox!</b>
            Количество доступных обновлений: {{ states('sensor.node_pve_total_updates') | default(0) }}.
            Проверьте систему и обновите при необходимости.

  # ----------------------------------------------------------------------------
  # Телеграм: Сейф — риск плесени (подтвержденный)
  # ----------------------------------------------------------------------------
  - id: "666e2076-ea49-4c53-85de-ef23566b5d00"
    alias: "Уведомление. Телеграм: Сейф — риск плесени"
    mode: single
    triggers:
      - trigger: state
        entity_id: input_boolean.strongbox_hum_text_notified
        to: "on"
    conditions:
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
    actions:
      - variables:
          rh: "{{ states('sensor.strongbox_th_humidity') | float(0) | round(1) }}"
          t: "{{ states('sensor.strongbox_th_temperature') | float(0) | round(1) }}"
          dp: "{{ states('sensor.seif_tochka_rosy_c') | default(states('sensor.strongbox_th_temperature')) | float(0) | round(1) }}"
          ah: "{{ states('sensor.seif_absolyutnaya_vlazhnost_g_m3') | default(0) | float(0) | round(2) }}"
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: >-
            ⚠️ <b>Сейф: риск плесени</b>
            Влажность: <b>{{ rh }}%</b>, Температура: <b>{{ t }}°C</b>
            Точка росы: <b>{{ dp }}°C</b>, Абс. влажн.: <b>{{ ah }} г/м³</b>

            Рекомендации:
            • Положите влагопоглотитель (силикагель) или замените пакет.
            • Проветрите сейф и помещение (без резких перепадов).
            • Стремитесь к RH 40–55% и T ≥ {{ states('input_number.strongbox_temp_min')|int }}°C.

  # ----------------------------------------------------------------------------
  # Телеграм: Сейф — условия нормализовались
  # ----------------------------------------------------------------------------
  - id: "cba8ab8b-5849-423a-bc56-6c04bfb51699"
    alias: "Уведомление. Телеграм: Сейф — условия нормализовались"
    mode: single
    triggers:
      - trigger: state
        entity_id: input_boolean.strongbox_hum_text_notified
        to: "off"
    conditions:
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
    actions:
      - variables:
          rh: "{{ states('sensor.strongbox_th_humidity') | float(0) | round(1) }}"
          t: "{{ states('sensor.strongbox_th_temperature') | float(0) | round(1) }}"
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: >-
            ✅ <b>Сейф: условия нормализованы</b>
            Текущие значения: RH <b>{{ rh }}%</b>, T <b>{{ t }}°C</b>.

  # ----------------------------------------------------------------------------
  # Очиститель — фильтр <10%
  # ----------------------------------------------------------------------------
  - id: "d3bb27a3-51be-4ce6-8dc7-ebad482e5ea3"
    alias: "Телеграм: очиститель — фильтр <10%"
    mode: single
    triggers:
      - trigger: numeric_state
        entity_id: sensor.zhimi_cn_271517251_mb3_filter_life_level_p_4_3
        below: 10
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
      - condition: state
        entity_id: input_boolean.air_filter_replace_notified
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: "⚠️ Очиститель воздуха: ресурс фильтра &lt; 10%."
      - action: input_boolean.turn_on
        target:
          entity_id: input_boolean.air_filter_replace_notified

  # ----------------------------------------------------------------------------
  # Увлажнитель — фильтр <10%
  # ----------------------------------------------------------------------------
  - id: "dbaaf929-4f15-4b23-8d4b-b8edd4083484"
    alias: "Телеграм: увлажнитель — фильтр <10%"
    mode: single
    triggers:
      - trigger: numeric_state
        entity_id: sensor.xiaomi_cn_823407205_p3_filter_life_level_p_8_1
        below: 10
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
      - condition: state
        entity_id: input_boolean.humidifier_filter_replace_notified
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: "⚠️ Увлажнитель: ресурс фильтра &lt; 10%."
      - action: input_boolean.turn_on
        target:
          entity_id: input_boolean.humidifier_filter_replace_notified

  # ----------------------------------------------------------------------------
  # Низкий уровень воды
  # ----------------------------------------------------------------------------
  - id: "68eb40de-2339-4260-9bd6-3db5aa1604d9"
    alias: "Телеграм: увлажнитель — низкий уровень воды"
    mode: single
    triggers:
      - trigger: state
        entity_id: event.xiaomi_cn_823407205_p3_low_water_level_e_2_1
    conditions:
      - condition: state
        entity_id: input_boolean.system_startup_phase
        state: "off"
      - condition: state
        entity_id: input_boolean.humidifier_low_water_notified
        state: "off"
    actions:
      - action: notify.send_message
        target:
          entity_id: notify.telegram_bot_5861567452_382517
        data:
          message: "💧 Увлажнитель: низкий уровень воды. Пожалуйста, долейте воду."
      - action: input_boolean.turn_on
        target:
          entity_id: input_boolean.humidifier_low_water_notified
